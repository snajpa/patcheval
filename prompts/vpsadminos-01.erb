KERNEL PATCH EVALUATION

For the following patch:

Commit Message:

<%= @params[:message] %>

Commit Diff:

<%= @params[:diff] %>


Analysis is required.

Exclude when the patch is mostly related to these Exclusion Categories:
- Local Disk-based Filesystems (like ext4, xfs, btrfs, f2fs, etc.)
- Graphics
- drm, display
- sound, audio, usb, media drivers
- wifi, nfc, bluetooth, other wireless drivers
- Other than x86-related peripherals
- Documentation, tests
End of Exclude Categories.

Include when the patch is related to Interest Areas:
+ Namespaces, Cgroups
+ Process management
+ Resource isolation
+ Networking protocols related fixes, TCP/IP stack, 10G/25G ethernet drivers
+ Netfilter
End of Interest Areas.

Evaluation Procedure:
- Check the patch for any of the Exclusion Categories.
- Check the patch for any of the Interest Areas.
- If the patch isn't in the Exclusion Categories and is in the Interest Areas, 'ACCEPT' the patch.
- If the patch is in the Exclusion Categories, 'IGNORE' the patch.
- 'IGNORE' all other patches.
End of Evaluation Procedure.

An outcome of this Analysis is required. Explanation is not required, but a single sentence is acceptable.

Outcome ('IGNORE'/'ACCEPT'):